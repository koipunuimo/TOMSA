class Vetor:
    def __init__(self):
        self.x = float(0)
        self.y = float(0)
        self.z = float(0)
    
    def __str__(self):
        return "x: " + str(self.x) +"\ny: " + str(self.y )+ "\nz: " + str(self.z)